require "rails_helper"

RSpec.describe PreferenciaController, type: :routing do
  describe "routing" do

    it "routes to #index" do
      expect(:get => "/preferencia/index").to route_to("preferencia#index")
    end

    it "routes to #new" do
      expect(:get => "/preferencia/new").to route_to("preferencia#new")
    end

    #it "routes to #show" do
    #  expect(:get => "/preferencia").to route_to("preferencia#show")#, :id => "1")
    #end

    #it "routes to #edit" do
    #  expect(:get => "/preferencia").to route_to("preferencia#edit", :id => "1")
    #end

    it "routes to #create" do
      expect(:post => "/preferencia").to route_to("preferencia#create")
    end

    #it "routes to #update via PUT" do
    #  expect(:put => "/preferencia/1").to route_to("preferencia#update", :id => "1")
    #end

    #it "routes to #update via PATCH" do
    #  expect(:patch => "/preferencia/1").to route_to("preferencia#update", :id => "1")
    #end

    #it "routes to #destroy" do
    #  expect(:delete => "/preferencia").to route_to("preferencia#destroy", :id => "1")
    #end

  end
end
